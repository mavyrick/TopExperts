# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TopExperts' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TopExperts

  target 'TopExpertsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TopExpertsUITests' do
    inherit! :search_paths
    # Pods for testing
  end

    pod 'ImageLoader'
    pod 'SwiftyJSON', '~> 4.0'
    pod 'Alamofire', '~> 4.7'

end
